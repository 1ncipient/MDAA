package analysis;

import selection.SelectionObject;

/**
 * Defines the interface for ComputeServer to implement.
 * @author Henry So
 *
 */
public interface Recalculate {
	public void doThis(SelectionObject select);
}
